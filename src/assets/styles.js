import styled from 'vue-styled-components';

const Card = styled.div`
    background-color: #042f3a;
    padding: 16px;
    border-radius: 8px;
    box-shadow: 1px solid rgba(0, 0, 0, .15);
    display: flex;
    flex-wrap: wrap;
    width: 100%;
    max-height: 100%;  
    padding: 10px auto
`;

export default Card;